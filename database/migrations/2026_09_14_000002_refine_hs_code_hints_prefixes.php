<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Support\Facades\DB;

/**
 * Живой тест сразу после первой миграции (2026_09_14_000001) вскрыл реальный
 * баг в алгоритме подбора: поиск Ozon по значению атрибута — НЕ префиксный,
 * а по вхождению подстроки где угодно в 10-значном коде (напр. запрос "4010"
 * матчил код "8703604010" — легковые авто, т.к. это его последние 4 цифры).
 * Плюс для "широких" 4-значных глав вроде 8511 (зажигание/пуск/генераторы —
 * ВСЕ вместе) и 8421 (все виды фильтрования, не только масляные/топливные)
 * эвристика "больше всего слова 'прочие'" стабильно попадала в СОСЕДНИЙ, а
 * не в нужный подраздел (напр. "Свеча зажигания" резолвилась в код
 * "Генераторы прочие, прочие" — та же 4-значная глава 8511, но не то).
 *
 * Фикс в самом коде (str_starts_with вместо str_contains) закрыл первую
 * проблему; эта миграция закрывает вторую — сужает префиксы для
 * неоднородных глав до реально проверенных живьём 6-значных подсубпозиций.
 */
return new class extends Migration
{
    private const UPDATES = [
        'свеча зажигания' => '851110', // подтверждено живьём: "8511100009 - Свечи зажигания прочие"
        'катушка зажигания' => '851130', // "Распределители, катушки зажигания..."
        'стартер' => '851190', // widerange 8511 попадал то в генераторы, то в авиацию — сузили до "части"
        'генератор' => '851190',
        'фильтр масляный' => '842123', // "Оборудование... для фильтрования масла или топлива"
        'фильтр топливный' => '842123',
        'фильтр воздушный' => '842131', // "Воздушные фильтры для двигателей внутреннего сгорания" — отдельная подсубпозиция
        'резонатор фильтра' => '842131',
    ];

    public function up(): void
    {
        foreach (self::UPDATES as $keyword => $newPrefix) {
            DB::table('hs_code_hints')
                ->where('keyword', $keyword)
                ->update(['tnved_prefix' => $newPrefix, 'updated_at' => now()]);
        }
    }

    public function down(): void
    {
        // Откатывать до широких 4-значных префиксов смысла нет — они были
        // источником самого бага, который эта миграция чинит.
    }
};
