# Progress — журнал текущей работы

Это не архитектурный справочник (для этого `CLAUDE.md`), а рабочий журнал:
что в работе прямо сейчас и на чём остановились. Роман — единственный
разработчик, может уйти в паузу на день или на неделю — этот файл экономит
время на пересказ контекста при возврате к задаче.

Правило: в начале сессии по этой задаче — читать секцию «Статус». В конце
сессии (или когда прерываемся) — обновлять «Статус» и дописывать запись в
«Историю» (дата + что сделали, кратко).

## Текущая задача

Причёсывание сайта силами агента (дизайн, мобильная адаптация) — идёт в фоне
параллельно с ERP, см. приоритеты в `CLAUDE.md`. Каталог на главной (блок
«Популярные категории» на `parts_catalog`) — базово сделан, детали ниже и в
`CLAUDE.md`, раздел «Открытые технические задачи», пункт 3.

## Статус (2026-08-12)

Последнее сделанное: страница гаража (`/garage` и `/garage/create`) на мобиле
не имела хедера/футера вообще — добавил `header-mini`/`footer-bar-mini`
инклюды на обе страницы (конвенция сайта, `layouts.app` их не подключает
сам). Первая попытка обернуть в стандартный `.main-wrapper` с
`padding-top:100px` на десктопе сломала десктопный вид — оказалось, что
`.garage` и `#garage-new-item-create` уже давно (до этой сессии) имели
собственный десктопный `margin-top:150px`, без него не было бы отступа от
фиксированного хедера. `.main-wrapper` добавлял отступ ПОВЕРХ уже
существующего — двойной отступ, "гараж уехал". Убрал `.main-wrapper` из
обеих вьюх, оставил их исходную структуру + просто добавленные инклюды.

На мобиле у той же старой десктопной CSS `#garage-new-item-create` был
`padding-left:180px;padding-right:300px` (480px суммарно) — на телефоне это
почти обнуляло ширину полей формы. Добавил в конец `master.css`
`@media(max-width:767px)` блок: обнуляет `margin-top` у `.garage` и
`#garage-new-item-create` (`header-mini` на мобиле sticky, не fixed —
отдельный отступ не нужен) и меняет катастрофичный padding на `16px` с
каждой стороны. Также переверстал список машин в `garage/index.blade.php`:
десктоп — прежняя таблица (`d-none d-md-table`), мобилка — карточки
(`d-md-none`, по одной на машину). `master.css` `?v=21` → `?v=22`.

Не проверено вживую (роуты за auth, curl не даёт полной картины) — Роман
должен глянуть `/garage` и `/garage/create` на десктопе и мобиле.

## Статус (архив, 2026-08-10)

Первая версия публичного каталога реализована и вручную протестирована через
curl на локальном `artisan serve` — работает (главная → категория → группа →
карточка, коды 200/404 корректные).

Готово:
- Миграция `category_top_code/title/slug` (path[3]) прогнана, бэкфилл 50010 строк.
- Новая миграция `2026_08_10_000001_add_group_category_to_parts_catalog_table`
  (уровень группы, path[4]) — прогнана, бэкфилл 48566 строк.
- `App\Http\Controllers\CatalogController` (публичный, без auth):
  `category($topSlug)`, `group($topSlug,$groupSlug)`, `show(PartsCatalog)`.
  Публичные запросы всегда фильтруют `scrape_status=done` + `name IS NOT NULL`
  (осознанно, чтобы не плодить тонкий контент — см. GSC-задачу в CLAUDE.md).
- Роуты: `catalog.category` (`/catalog/{topSlug}`), `catalog.group`
  (`/catalog/{topSlug}/{groupSlug}`), `catalog.show` (`/catalog/card/{id}`,
  специально в отдельном неймспейсе `card/`, чтобы не конфликтовать по
  количеству сегментов с `catalog.group`).
- Views: `resources/views/catalog/{category,group,show}.blade.php` +
  партиал `_card.blade.php`. `show.blade.php` — публичная версия
  `admin/parts-catalog/show.blade.php` (без auth, без debug JSON, с SEO
  title/description/canonical по образцу `global_product.blade.php`).
- `HomeController::index()` — топ-6 категорий по `COUNT(*)`, замена
  захардкоженного блока в `index.blade.php`. Константа `CATEGORY_ICONS` в
  `HomeController` мапит `category_top_code → путь к иконке`; для категорий
  без иконки — фолбэк `images/placeholders/default_gear.jpeg`.

Известный побочный момент (не блокирует, но стоит знать): при прогоне миграций
обнаружился дрейф схемы — в `parts_catalog` уже были колонки
`kaspi_category_path` (json) и `ozon_category_id` (bigint), добавленные мимо
Laravel-миграций (файла миграции для них нет ни в репо, ни в
`database/migrations.tar.gz`). Не трогали, но если будешь разбираться в схеме
таблицы — учти, что `migrate:status`/файлы миграций не показывают полную
картину состояния этой таблицы.

Не сделано (осознанно отложено, не спрашивал повторно раз не просили):
- Иконки для 3 из 6 категорий в топе (Трансмиссия, Система охлаждения,
  Рулевое управление) — сейчас на дефолтной шестерёнке. Просто брось файлы
  `*.png` в `public/images/popular-categories/` и добавь запись в
  `HomeController::CATEGORY_ICONS` по коду категории (см. список кодов там же).
- SEO-слаг в URL карточки (`catalog.show` сейчас чисто по id: `/catalog/card/122486`,
  без человекочитаемого артикула в пути) — можно добавить позже, не критично
  для первой версии.

Роман глянул страницы вживую в браузере и нашёл два реальных дефекта, которых
curl-проверка не ловит:
1. Гигантские неоформленные стрелки пагинации — `$cards->links()` по умолчанию
   рендерит `pagination::default` (tailwind-вьюха с inline SVG), а сайт на
   Bootstrap, tailwind-классы не подгружены → SVG рендерится в исходном
   размере. Исправлено: везде `$cards->links('pagination::bootstrap-5')`
   (`catalog/category.blade.php`, `catalog/group.blade.php`, и заодно
   `admin/parts-catalog/index.blade.php` — та же болячка).
2. Не было хедера/футера сайта. Причина: `layouts.app` их не подключает вообще
   (только `@yield('content')`) — на этом сайте каждая публичная страница сама
   делает `@include('components.header')` + `@include('components.header-mini')`
   в начале контента и `@include('components.footer-bar-mini')` +
   `@include('components.footer')` в конце (см. `global_product.blade.php`,
   `cart.blade.php` — это конвенция, не баг архитектуры). Плюс нужен
   `.main-wrapper { padding-top: 100px }` на десктопе (0 на мобиле), иначе
   фиксированный хедер перекрывает контент — скопировал точный рабочий блок
   стилей из `global_product.blade.php`. Добавлено во все 3 catalog-view.

Дальше Роман глянул карточку товара и сказал, что она выглядит хуже, чем сама
карточка на Kaspi — переделал `catalog/show.blade.php` под двухколоночный
современный вид (галерея слева, sticky инфо-панель справа с бейджами
артикул/бренд, блок «быстрых фактов» — авто-извлечение полей с флагом
`important` из `characteristics->specifications`, не хардкод под конкретную
категорию), и табы Описание/Характеристики/Применимость вместо плоских таблиц
подряд. Вместо кнопки — тогда ещё была WhatsApp-CTA «Узнать цену», т.к. цены
в parts_catalog никогда не было.

Затем Роман сказал: это живой каталог, нужна цена и «Добавить в корзину»
и в сетке категории/группы, и на карточке — ради быстрой конверсии, WhatsApp
как основной CTA не годится. Это потребовало реального источника цены:

- Рассматривали `global_catalog` (готовая таблица с ценами, 253k строк) — НЕ
  взяли: `updated_at` там максимум 2026-05-20, т.е. цены зависшие на 3 месяца.
  Показывать это как актуальную цену для "Добавить в корзину" на живом сайте —
  прямой риск продать не по той цене.
- Взяли `supplier_offers` — тот же стол, что ежедневно обновляет пайплайн
  `prices:fetch → offers:aggregate` (см. CLAUDE.md), `updated_at` реально
  сегодняшний. Матчинг с `parts_catalog` по `article_normalized`/
  `brand_normalized` даёт 91% покрытия (45718 из 50091), проверено вручную.
- Добавил миграцию `2026_08_10_000002_add_normalized_columns_to_supplier_offers_table`
  — generated STORED колонки `sku_normalized`/`brand_normalized` на
  `supplier_offers` (та же нормализация, что и в
  `SeedOwnPartsCatalogCommand::normalizeArticle/normalizeBrand`) + составной
  индекс. Без индекса JOIN с REGEXP_REPLACE "на лету" по 121k строк давал
  ~300-800ms на страницу; generated-колонки — потому что тогда синхронизация
  с `supplier_offers` не зависит от правок в `AggregateSupplierOffersCommand`,
  БД сама пересчитывает при каждой вставке/апдейте. С индексом JOIN — ~13ms.
- `CatalogController::attachOffers()` — для каждой карточки берёт самый
  дешёвый оффер СРЕДИ ОФФЕРОВ С STOCK>0, если таких нет — просто самый дешёвый
  (для инфо, без кнопки корзины). Цена продажи — `KaspiPriceCalculator::calculate()`,
  тот же движок наценки, что и в реальном Kaspi-пайплайне (комиссия/налог/
  логистика/маржа), не выдуманная накрутка.
- Кнопка "Купить"/"Добавить в корзину" — переиспользовал существующий
  sitewide-обработчик `.api-buy-btn` из `public/js/master.js` (уже подключён
  везде через layout, ничего нового в JS не писал) + существующий эндпоинт
  `POST /cart/add-api` (`GlobalProductController::addToCartApi`) и класс
  `App\Cart` — то есть корзина ровно та же, что и на остальном сайте, не
  отдельная параллельная реализация.
- Если совпадения с supplier_offers нет вообще (~9% карточек) — цена не
  показывается («Цена по запросу»), WhatsApp-CTA остаётся резервным вариантом.
  Если совпадение есть, но stock=0 — показываем цену как информацию + "Под
  заказ (N дн.)" и WhatsApp вместо кнопки корзины (кнопка "Купить" по всему
  сайту и так блокирует добавление при qty<=0 в самом JS — не стал с этим
  спорить, просто не показываю кнопку заранее вместо алерта).

Проверено на localhost: группа `engine/belt-and-chain-drives` — 45/48 карточек
с ценой и кнопкой "Купить", 3 без (корректно). Карточка MASUMA MGR2010 —
89 946 ₸, data-атрибуты кнопки корректные (article/brand/price/qty/supplier).
Страница группы отдаётся за ~380ms с учётом джойна цен — приемлемо.

Дальше поправили две мелочи:
- Иконки `bi bi-*`/`fas fa-*` нигде на сайте не подключались (ни CDN, ни файлы
  шрифтов в `master.css`) — WhatsApp-кнопка и аватарка в хедере всегда были
  просто цветными кружками без глифа. Это баг всего сайта, не только каталога.
  Добавил Bootstrap Icons + Font Awesome CDN в `layouts/app.blade.php` (один
  раз, глобально).
- Блок на главной вместо топ-6 теперь показывает ВСЕ категории (сейчас 14,
  лимит в запросе поднят до 20 про запас) — Роман сравнил с конкурентом
  (cartex.kz показывает 15 плиток) и решил не резать список. Иконки под
  категории без готовой картинки — дефолтная шестерёнка (`CATEGORY_ICONS` в
  `HomeController`), Роман сделает иконки позже сам.

Затем попросили центрировать неполный последний ряд плиток (и на главной, и в
сетке каталога) — на любом размере экрана. В каталоге (`category.blade.php`,
`group.blade.php`) это просто `justify-content-center` на Bootstrap `.row`
(flex под капотом). На главной оказалось сложнее и я сначала промахнулся:
правил `public/css/main1.css`/`main-mini1.css` (сетка `#popular-categories-
container`), но эти файлы **нигде не подключены** — `index.blade.php` тянет
`css/main.css`/`css/main-mini.css`, а этих файлов **физически нет** в
`public/css/` (мёртвые `<link>`, 404, браузер их просто игнорирует). Реально
работает только `master.css` (единый монолитный файл, `layouts/app.blade.php`,
с `?v=N` кэш-бастингом) — в нём был забандлен старый вариант того же правила.
Поправил `master.css` напрямую (grid → flex-wrap + justify-content:center),
убрал мёртвые `<link>` на main.css/main-mini.css из `index.blade.php`, поднял
`?v=3` → `?v=5` по ходу (дважды, второй раз — когда после первого фикса
плитки на широком экране "слиплись": вместо `flex-basis: calc(...)` привязанного
к предполагаемому числу колонок поставил фиксированную ширину (150px десктоп/
96px мобайл) + реальный `padding` внутри `.popular-categories-item` — так не
нужно угадывать, сколько плиток влезет в ряд, всегда есть отступ).
**main1.css/main-mini1.css остались висеть неиспользуемыми — не удалял, не
спрашивал явно.**

Затем Роман поймал ценовое расхождение: карточка каталога считала цену через
`KaspiPriceCalculator::calculate()` (комиссия Kaspi 12.5% + логистика Kaspi
Доставки зашиты в формулу) — а обычный поиск на сайте (`SparePartController::
getSearchedPartAndCrosses` → `setPrice()`) эти издержки не добавляет, т.к. это
прямая продажа, не через Kaspi Marketplace. Итог — цена в каталоге была
заметно выше цены на ту же позицию в поиске, что подрывает доверие при
сравнении клиентом. Поменял `CatalogController::attachOffers()` на
`(new SparePartController())->setPrice()` — тот же расчёт, что и в поиске,
включая учёт роли пользователя (опт/админ). На примере MASUMA MGR2010: было
89 946 ₸ (Kaspi-калькулятор) → стало 73 623 ₸ (setPrice). См. подробности в
CLAUDE.md, там же зафиксировано явно "НЕ KaspiPriceCalculator", чтобы не
наступить на те же грабли снова.

## Следующий шаг

Роман должен посмотреть глазами: цены (теперь пересчитанные — стоит свериться
с обычным поиском на конкретном артикуле, что цифры сходятся) и кнопки "Купить"
в сетке категории и на карточке товара, попробовать реально добавить товар в
корзину и проверить, что он появляется в `/cart` с правильной ценой и
поставщиком. Плюс глянуть центрирование на главной и в каталоге на разных
экранах. Если ок — обсуждать дальше оформление и, возможно, бейдж "под заказ"
на самой плитке сетки каталога (сейчас это просто текст, без визуального
акцента).

## Побочная ветка: общий дизайн сайта (2026-08-10)

Не часть задачи каталога, но в этой же сессии. Роман спросил сравнить дизайн
с конкурентом (cartex.kz) — по коду (не визуально) нашёл: `--bs-primary` был
дефолтным Bootstrap-синим (#0d6efd, никогда не переопределялся), на одной
главной+хедере использовалось 7+ разных Bootstrap-цветов вперемешку, шрифта
своего нет вообще (`@font-face` — 0 совпадений в master.css, чистый системный
стек). Сделали:

- Пиксель-сэмплингом (`PIL`) вытащили точные цвета из `public/images/logo1.png`
  (лого НЕ трогали, только взяли его палитру): тёмно-синий `#042D4D`, красный
  `#DA251C`.
- В конец `public/css/master.css` добавили блок переопределения:
  `--bs-primary`→красный (кнопки/CTA/ссылки сайта, включая мои
  `.api-buy-btn`), `--bs-dark`→тёмно-синий (хедер/футер/тёмные поверхности).
  Переопределяли и переменные, и сами `.btn-primary`/`.btn-dark` (в Bootstrap
  5.3 у кнопок цвета захардкожены в собственных `--bs-btn-*`, а не тянутся из
  корневых `--bs-primary` — без явного override кнопки бы не подхватили).
- Шрифт Golos Text (Google Fonts, хорошая кириллица) — подключили в
  `layouts/app.blade.php`, назначили через `--bs-body-font-family`.
- `master.css` версия `?v=3` → `?v=6` по ходу всей сессии (кэш-бастинг).

Иконки категорий (`public/images/popular-categories/`) — осознанно НЕ трогаем,
Роман сгенерирует сам через нейросеть в едином стиле. С меня — промпт для
генерации (ещё не написан, попросит отдельно в конце).

Дальше пошла итеративная доводка палитры прямо по ходу просмотра (Роман
смотрел глазами, что не понравится — правили сразу):

1. Поисковая строка в хедере (рамка + кнопка "Найти") — отдельный override в
   тёмно-синий, конкретно эта не понравилась красной (соседствует с красными
   Kaspi-бейджами в топ-баре). `#input-searchbtn-wrapper` в `master.css`,
   независимо от общего `--bs-primary`.
2. Некрасные/не-WhatsApp зелёные CTA ("Подобрать по VIN", кнопка отправки
   VIN-формы, "Позвонить") переведены с `btn-success`/`btn-outline-secondary`
   на `btn-primary` — настоящие WhatsApp-кнопки (с иконкой/wa.me) остались
   зелёными осознанно, это не декоративный цвет, а узнаваемый брендом
   WhatsApp паттерн.
3. Пробовали `--bs-primary` = navy вместо красного ("navy основа везде") —
   Роман посмотрел, показалось тяжеловато на весь сайт.
4. Emoji-иконки в блоках "3 шага"/"Почему нас выбирают" (📸🔍📦🔧✅💬💰🔄,
   были на `text-primary`/`text-success`/`text-danger`/`text-info`/
   `text-warning`/`text-secondary`) заменены на Font Awesome (`fas fa-camera`
   и т.д.) — **важный технический момент**: emoji игнорируют CSS `color`
   полностью (это цветные битмап/COLR-глифы), так что все те классы раньше
   вообще ничего не делали, разноцветность была из самих emoji, не из
   Bootstrap-классов. Только на иконках (не на тексте/шрифте) CSS-цвет
   реально что-то может поменять.
5. Эти 9 иконок посадили на `text-dark` (не `text-primary`) — специально,
   чтобы не зависели от переключений `--bs-primary` между красным/navy
   (Роман ещё колебался). `--bs-dark` весь этот заход остаётся тёмно-синим
   константно, это "стабильный" якорь палитры.
6. Финально `--bs-primary` вернули на красный (был запрос попробовать оба
   варианта) — кнопки/CTA снова красные, иконки остались синими (п.5 это и
   обеспечивает), поисковая строка синяя (п.1 не зависит от primary).

Текущее состояние палитры (2026-08-10, конец сессии): `--bs-primary` = красный
`#DA251C` (кнопки/CTA), `--bs-dark` = тёмно-синий `#042D4D` (иконки, хедер,
футер, поисковая строка). Переключить снова на navy — 4 строки в конце
`master.css` в блоке с пометкой "ВЕРСИЯ 2". `master.css` версия `?v=3` → `?v=10`
за сессию.

## История

### 2026-08-10
Зафиксировали задачу в `CLAUDE.md`. Реализовали первую версию публичного
каталога на parts_catalog: 2 миграции (top+group категории), CatalogController
+ 3 роута + 3 view, обновили HomeController и блок на главной. Проверено curl'ом
на localhost — статусы верные, но живьём в браузере Роман нашёл: гигантские
tailwind-стрелки пагинации (нет bootstrap-вьюхи) и отсутствие хедера/футера
(layouts.app их не подключает, это делает каждая страница сама). Оба фикса
внесены и перепроверены. Затем переделал карточку товара в более современный
вид (галерея+sticky-панель+табы) по образцу Kaspi. Затем добавили реальные
цены и "Добавить в корзину" везде — источник цены `supplier_offers`
(ежедневно свежий, не `global_catalog` — тот зависший на 3 месяца), новая
миграция с generated-колонками для быстрого индексного джойна, переиспользовали
существующий sitewide cart JS/API вместо новой реализации.

### 2026-08-11
Дизайн сайта: извлекли точную палитру из лого (навy `#042D4D`, красный
`#DA251C`), шрифт Golos Text, починили Bootstrap Icons/FontAwesome (нигде
не были подключены), сгенерировали AI-иконки для 14 категорий главной + 6
подкатегорий тормозной системы. Переверстали `partSearchRes.blade.php`
(живой поиск): единая AJAX-модалка `#ajaxInfoBlock` для показа инфы по
товару у поставщиков кроме Gerat (раньше такой модалки для них не было
вообще), бейджи доставки классом `.gp-delivery-badge`, мобильная версия
`info-block`/carousel (раньше вообще не имела мобильных стилей).

### 2026-08-12
Гараж (`/garage`, `/garage/create`) — не было хедера/футера на мобиле,
затем после первого фикса поехала десктопная верстка (двойной
`margin-top` поверх старого десктопного отступа `.garage`/
`#garage-new-item-create`) и мобильная форма создания авто ломалась из-за
унаследованного `padding-left:180px;padding-right:300px`. Убрал
конфликтующий `.main-wrapper`, добавил `@media(max-width:767px)`
override в `master.css`, переверстал список машин в карточки на мобиле.
`master.css` `?v=22`.
